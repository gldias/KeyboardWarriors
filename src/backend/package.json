{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/api.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.2",
    "@types/jest": "^24.9.1",
    "@types/node": "^12.12.26",
    "typescript": "^3.7.5"
  },
  "devDependencies": {
    "tslint": "^6.0.0"
  }
}
